extends layout 

block content 
  h1= title

  p Enter the correct PIN to receive membership perks

  form(action="" method="POST")
    .form-group 
      input#pin(type="text" name="pin" maxlength="6") 
    button.btn(type="submit") Enter